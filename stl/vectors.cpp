#include<bits/stdc++.h>
using namespace std;
int main(){
    // vector implementation
    vector<int> v;
    // A vector is a dynamic array. It is similar to an array but doesn't have a fixed size.
    // It can grow or shrink its size.
    // It is a sequence container and also known as a Dynamic Array or Array List.
}