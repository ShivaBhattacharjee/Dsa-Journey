#include <iostream> // Include the iostream header, which provides input/output stream functionality


// some extra libraries

#include<math.h> //For mathematical function 
#include <string> //for string related function 
using namespace std; // Use the namespace std, so we can directly access its members without prefixing them with std::

int main()
{                          // The main function is the entry point of the program
    cout << "Hello world"; // Output the string "Hello world" to the standard output stream
    return 0;              // Return 0 to indicate successful program execution
}
